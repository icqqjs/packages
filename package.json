{
  "name": "icqqjs",
  "version": "0.0.0",
  "description": "icqq 对外包的 monorepo：CLI、SDK、Gateway 统一管理",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/icqqjs/packages.git"
  },
  "author": {
    "name": "凉菜",
    "email": "admin@liucl.cn"
  },
  "license": "ISC",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.0.2",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:coverage": "pnpm --filter @icqqjs/cli test:coverage",
    "generate:skill-map": "pnpm --filter @icqqjs/cli generate:skill-map"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "express": "^5.2.1",
    "react": "^19.0.0"
  }
}
