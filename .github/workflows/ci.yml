name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # 先构建：sdk/gateway 的 typecheck 依赖 @icqqjs/cli 的 dist 声明文件（dist 不入库）
      - run: pnpm build

      - run: pnpm typecheck

      - run: pnpm test:coverage

      - run: pnpm --filter @icqqjs/sdk --filter @icqqjs/gateway test

      - run: pnpm --filter @icqqjs/cli generate:skill-map --check
